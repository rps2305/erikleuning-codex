# Implementation Notes: Menu Home Label and Width Alignment

## Baseline measurements (before changes)

- Header container: `.site-header__container` uses `width: min(1200px, 92vw)` in `assets/css/main.css`.
- Main content: `main` uses `width: min(1100px, 92vw)` in `assets/css/main.css`.
- Page wrapper: `.page-wrapper` uses `width: min(1100px, 92vw)` in `assets/css/main.css`.
- Content shell variable: `--content-max` is `1100px` in `assets/css/main.css`.
